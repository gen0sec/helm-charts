{{- if and .Values.operator.enabled .Values.operator.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default "moat-operator" .Values.operator.serviceAccount.name }}
  namespace: {{ .Values.global.namespaces.operator }}
  labels:
    app.kubernetes.io/name: moat-operator
{{- end }}
